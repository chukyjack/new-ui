export interface Student {
    id: number;
    first_name: string;
    last_name: string;
    name: string;
    course: string;
    address: string;
    contact: string;
}

export interface Associates {
    value: number;
    label: string;
}
